name: Docs

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  docs:
    name: Build and deploy docs
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0   # full history for git-revision-date if added later

      - uses: actions/setup-python@v7
        with:
          # Pinned rather than "3.x": this is only a launcher for the tooling
          # below, and a fixed interpreter keeps a green run reproducible.
          python-version: "3.13"

      - name: Install MkDocs + Material
        run: pip install -r docs/requirements.txt

      - name: Build docs (PRs — verify no broken build)
        if: github.event_name == 'pull_request'
        run: mkdocs build --strict

      - name: Deploy to GitHub Pages
        if: github.event_name != 'pull_request'
        run: mkdocs gh-deploy --force
